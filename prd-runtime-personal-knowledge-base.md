# PRD: Runtime 个人知识库（Personal Knowledge Base）

> **Parent Document**: [Product Requirements Document (CrewAgent)](prd.md)
> **Scope Boundary**: 本文仅覆盖个人知识库（跨项目长期记忆）；MVP 支持 `chat/agent/run` 三模式的显式记忆入库（候选 -> 确认 -> 提交），但检索与注入仅在 `chat` 模式启用；不包含项目知识库与企业级 MCP-RAG。

## 1. 概述

本 PRD 定义 CrewAgent Runtime 的个人知识库能力。目标是在不引入复杂平台依赖的前提下，建立一套可持续积累、可检索、可治理、可审计的个人长期记忆机制。

个人知识库用于沉淀用户长期偏好与助手长期行为约束，不用于存放项目临时信息。知识以本地文件为真相源，以本地索引为检索加速层。MVP 阶段在 `chat/agent/run` 三模式均可触发显式入库，但仅 `chat` 模式会在会话构建时读取并注入个人知识。

---

## 2. 用户故事 (User Stories)

- 作为用户，我希望助手记住我长期稳定的偏好（称呼、语言风格、边界），避免每次重复说明。
- 作为用户，我希望“记住这条信息”是显式可控的，而不是助手自动乱记。
- 作为用户，我希望只通过简单按钮完成“清空个人记忆”和“重新整理记忆”，不需要理解文件结构。
- 作为离线场景使用者，我希望个人知识全部保存在本地，不依赖云端服务。

---

## 3. 问题定义 (Problem Definition)

当前 Runtime 缺少标准化个人记忆机制，导致：

1. 长期偏好无法稳定继承，跨会话重复沟通成本高；
2. 记忆边界不清晰，容易把临时对话误当长期事实；
3. 缺少统一存储与检索结构，后续难以治理与审计；
4. 缺少“用户确认”的入库门槛，存在记忆污染风险。

---

## 4. 功能范围 (Scope)

### In Scope (MVP)

- 提供个人知识存储目录（本地）与标准文件结构；
- 支持“候选记忆 -> 用户确认 -> 落盘”写入流程（`chat/agent/run` 三模式）；
- 使用 LLM 做记忆语义路由（目标文件与候选内容抽取），并由系统执行硬约束校验；
- 支持个人知识检索与引用（仅 `chat` 模式回答前上下文增强）；
- 支持简化治理操作：清空个人记忆与重建索引（重新整理）；
- `chat` 模式会话构建时加载个人核心记忆（限额注入）。

### Out of Scope (MVP)

- 自动无确认写入（silent memory）；
- 跨设备云同步；
- 企业组织级策略编排；
- 与项目知识/共享知识包自动互转；
- `agent` 模式读取或注入个人知识；
- `run` 模式读取或注入个人知识。
- 文件级/条目级的手工编辑器（MVP 不暴露给普通用户）。

---

## 5. 验收标准 (Acceptance Criteria)

### AC-PK-1: 个人知识目录与文件结构

系统初始化后，必须存在个人知识目录（建议映射到 Runtime `@state` 范围），并支持以下文件结构：

- `USER.md`：用户称呼、语言、时区、沟通偏好；
- `SOUL.md`：助手行为边界与核心原则；
- `MEMORY.md`：长期稳定记忆；
- `memory/YYYY-MM-DD.md`：当日记忆流水；
- `index.json`：本地检索/统计元数据（加速层，不是事实源）。

### AC-PK-2: 显式入库确认

- 当用户在 `chat/agent/run` 任一模式表达“记住这个”“以后都按这个方式”等意图时，系统必须生成候选记忆；
- 候选生成前，系统必须先执行 LLM 路由并返回结构化结果（至少包含 `targetFile`、`candidateText`、`confidence`）；
- 落盘前必须得到用户明确确认；
- 未确认候选不得写入个人知识文件。

### AC-PK-3: 记忆分类与写入策略

- 长期稳定偏好优先写入 `SOUL.md` / `USER.md` / `MEMORY.md#Pinned`；
- 可复用但不要求固定生效的长尾记忆写入 `MEMORY.md#General`；
- 当日临时上下文优先写入 `memory/YYYY-MM-DD.md`；
- `targetFile` 必须通过系统白名单校验（仅允许 `USER.md`/`SOUL.md`/`MEMORY.md`/`memory/*.md`）；
- 当 `targetFile=MEMORY.md` 时，系统必须进一步区分 `targetSection=Pinned|General`，不得默认全部写入 `Pinned`；
- 当 LLM 路由低置信或输出非法结构时，系统必须走兜底策略（追问确认或规则降级），不得直接写入；
- 每条写入必须包含 `source`（用户确认/系统整理）与 `updatedAt`（时间戳）。

### AC-PK-4: 会话注入策略（Chat-only）

- 仅在 `chat` 模式会话构建时启用双层注入：
  - 固定层：始终注入 `SOUL.md` + `USER.md` + `MEMORY.md#Pinned`（如存在）；
  - 检索层：按 query 从 `MEMORY.md#General`（及 legacy non-pinned）与最近两份 non-empty daily memory 做 top-k 注入；
- 固定层与检索层分别受字符或 token 预算控制；
- 固定层命中且检索层 miss 时仍可注入；仅当两层都无有效内容时判定 miss；
- `agent` 与 `run` 模式不得读取、检索、注入个人知识内容。

### AC-PK-5: 简化治理操作（清空/整理）

- Settings 仅提供两个用户操作：`清空个人记忆`、`重新整理记忆`（重建索引）；
- `清空个人记忆` 必须二次确认，且执行后清空 `USER.md`、`SOUL.md`、`MEMORY.md` 与 `memory/*.md` 内容并重置索引；
- `重新整理记忆` 执行后可从 Markdown 真相源重建索引；
- 操作结果需以简单文案反馈（成功/失败），并记录审计日志。

### AC-PK-6: 隐私与隔离

- 个人知识默认仅本地可见，不自动上传第三方服务；
- 个人知识不得默认注入到其他用户会话；
- 访问权限遵循 Runtime 本地文件边界（只在允许目录读写）。

### AC-PK-7: 模式隔离与可预测性

- 当会话模式为 `chat` 时，个人知识支持“显式入库 + 双层注入（固定层+检索层）”；
- 当会话模式为 `agent` 或 `run` 时，仅支持“显式入库”，必须禁用“检索注入”；
- 在日志与审计中可区分“跨模式入库提交”与“chat 命中注入 / agent-run 跳过注入”事件。

---

## 6. 技术设计 (Technical Design)

### 6.1 存储布局

建议目录（示意）：

```text
@state/kb/personal/
  USER.md
  SOUL.md
  MEMORY.md
  memory/
    2026-03-09.md
  index.json
  manifest.json
```

`manifest.json` 建议最小字段：

```json
{
  "version": "1.0",
  "lastIndexedAt": "2026-03-09T00:00:00Z",
  "files": [
    { "path": "SOUL.md", "sha256": "...", "updatedAt": "..." }
  ]
}
```

### 6.2 入库流程

1. 在 `chat/agent/run` 三模式的用户输入入口统一触发“记忆候选抽取”；
2. 调用 LLM 路由器生成结构化候选（`targetFile/candidateText/confidence/reason`）；
3. 系统执行白名单与结构校验；低置信或非法结果进入兜底策略（追问或规则降级）；
4. 用户确认后写入目标 Markdown；
5. 写入完成后刷新 `index.json`；
6. 记录操作审计（写入来源、时间、文件、路由置信度）。

### 6.3 检索流程

1. 在 `chat` 模式先构建固定层注入（`SOUL.md` + `USER.md` + `MEMORY.md#Pinned`）；
2. 再按 query 触发检索层（`MEMORY.md#General` + recent non-empty daily memory）；
3. 检索命中后返回精简片段并附来源文件路径；
4. 以“固定层优先 + 检索层补充”的方式注入本轮上下文。

### 6.4 模式接入规则

1. `chat` 模式：启用显式入库、检索与注入；
2. `agent` 模式：启用显式入库，强制跳过检索与注入；
3. `run` 模式：启用显式入库，强制跳过检索与注入；
4. 模式切换后，按新模式重新计算“入库/注入”能力矩阵。

### 6.5 工具层建议（供实现）

- `kb.personal.addCandidate`
- `kb.personal.commit`
- `kb.personal.search`
- `kb.personal.get`
- `kb.personal.rebuildIndex`
- `kb.personal.clearAll`

注：MVP 也可先通过现有 `fs.*` + 内部服务实现，无需立即暴露全部工具。

---

## 7. 非功能需求 (Non-Functional Requirements)

- **NFR-PK-01 可靠性**：单次写入失败不得破坏原有知识文件；
- **NFR-PK-02 性能**：常见 query 检索响应目标 < 300ms（本地索引命中）；
- **NFR-PK-03 可观测性**：记录写入、删除、索引重建日志；
- **NFR-PK-04 可维护性**：Markdown 作为事实源，可脱离索引独立使用；
- **NFR-PK-05 安全性**：敏感信息默认不外发，导出需用户显式操作；
- **NFR-PK-06 可预测性**：不同会话模式下行为一致（`chat/agent/run` 均可显式入库；仅 `chat` 可检索注入）。
- **NFR-PK-07 路由可控性**：LLM 路由必须输出结构化结果并经过系统硬校验；模型异常不可导致越权写入。

---

## 8. 验证计划 (Validation Plan)

### 8.1 功能验证

- 候选记忆确认后正确写入目标文件；
- 拒绝确认时不写入；
- 清空个人记忆后检索结果同步清空；
- 重建索引后检索可恢复。

### 8.2 场景验证

- 连续 7 天日常对话后，助手可稳定复用长期偏好；
- 用户修改偏好后，新偏好在下一轮立即生效；
- 离线环境下个人知识完整可用。

### 8.3 回归验证

- 与现有会话流程兼容，不影响无知识库用户；
- 超长记忆文件下不会导致上下文爆炸；
- 文件损坏时有可恢复路径；
- `agent/run` 模式验证可触发显式入库，但不触发个人知识检索与注入；
- LLM 路由低置信、超时、结构错误场景下，系统可稳定降级且不误写入。

---

## 9. 路线图 (Roadmap)

| 阶段 | 范围 | 目标 |
| :--- | :--- | :--- |
| Phase 1 | 三模式显式入库 + 本地文件结构 + `chat` 双层注入（固定层+检索层） | 跑通个人知识闭环并保证注入边界可控 |
| Phase 2 | 极简治理 UI（清空/重建）+ 审计日志 | 面向非技术用户降低操作门槛 |
| Phase 3 | 智能整理（daily -> long-term）+ 质量评分 | 提升记忆质量与可维护性 |
