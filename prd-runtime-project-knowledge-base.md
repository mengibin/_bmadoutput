# PRD: Runtime 当前项目知识库（Project Knowledge Base）

> **Parent Document**: [Product Requirements Document (CrewAgent)](prd.md)
> **Scope Boundary**: 本文仅覆盖“当前项目知识库”；不包含企业级 MCP-RAG 与 `.bmad` 共享知识包治理。

## 1. 概述

本 PRD 定义 Runtime 当前项目知识库能力。目标是在每个项目内建立独立、本地、可检索的知识空间，用于沉淀导入资料与项目实践过程知识，支持后续执行阶段的稳定复用。

项目知识库遵循“项目内隔离、逐步沉淀、按需检索”原则：新项目初始化为空库，用户按需导入文件（PDF、Word、Markdown、图片等），系统完成提取、切片、索引，并在会话中按 query 返回相关片段。知识库存储于 Runtime 私有目录（非项目根目录），默认不提供手工改写入口；同时支持“孤儿项目清理”和“迁移（导出/导入）”。

---

## 2. 用户故事 (User Stories)

- 作为项目执行者，我希望新建项目时自动拥有空的知识库目录，便于后续统一管理参考资料。
- 作为用户，我希望把 PDF、Word 等资料拖入项目知识库后，助手能在执行中检索并使用这些内容。
- 作为用户，我希望项目过程中形成的决策、术语、约束能持续沉淀，不只停留在聊天记录里。
- 作为用户，我希望新建同类型项目时复用历史项目知识，但不污染当前项目特有信息。
- 作为用户，我希望当项目工程目录已不存在时，系统能自动识别并提示清理残留聊天与知识库数据。
- 作为用户，我希望项目知识库可以导出并迁移到另一个项目或另一台设备，保留可追溯元数据。

---

## 3. 问题定义 (Problem Definition)

当前 Runtime 缺少项目级知识容器，导致：

1. 资料输入分散在项目目录各处，缺少统一入口；
2. PDF/Word 等非纯文本资料无法稳定复用；
3. 项目执行过程中的高价值结论难以长期保存与检索；
4. 新项目复用历史经验时缺少明确迁移机制。
5. 缺少标准化“孤儿检测清理/迁移”能力，知识生命周期管理不完整。

---

## 4. 功能范围 (Scope)

### In Scope (MVP)

- 新项目创建时初始化项目知识库目录（空目录，位于 Runtime 私有存储）；
- 支持导入项目参考资料（PDF、DOCX、MD、TXT、图片）；
- 支持统一提取、切片、索引与检索；
- 支持把项目执行过程知识沉淀为结构化笔记；
- 支持“从历史项目导入知识快照（复制模式）”。
- 支持孤儿项目知识库清理（检测项目目录丢失后，用户确认删除残留数据）；
- 支持项目知识库迁移（导出/导入）。

### Out of Scope (MVP)

- 跨项目实时联动（A 项目知识变更自动同步到 B）；
- 企业共享知识库治理与权限系统；
- 完整版本合并与三方冲突自动解决；
- 在线向量服务强依赖（MVP 以本地索引为主）。

---

## 5. 验收标准 (Acceptance Criteria)

### AC-PJ-1: 新项目空知识库初始化

- 新增项目后，系统自动创建项目知识库目录（位于 Runtime 私有目录，不在项目根目录）；
- 初始化时目录为空，仅包含基础结构与清单文件；
- 不自动灌入历史项目知识。

### AC-PJ-2: 资料导入能力

- 用户可将文件导入项目知识库（UI 导入或目录投放）；
- MVP 必须支持：`.pdf`, `.docx`, `.md`, `.txt`, `.png`, `.jpg`, `.jpeg`, `.webp`；
- 导入后每个文件必须有唯一来源记录（文件名、导入时间、来源路径）。

### AC-PJ-3: 内容提取与标准化

- PDF/图片通过统一多模态路径提取文本或结构化片段；
- DOCX 可提取正文文本（保留基础段落结构）；
- 提取结果统一写入标准化文本层（用于切片与检索）；
- 提取失败文件必须返回明确错误并支持重试。

### AC-PJ-4: 检索与引用

- 对项目问题发起检索时，系统返回相关片段及来源信息；
- 每个命中结果至少包含 `sourceFile`、`snippet`、`score`；
- 会话注入按“命中片段优先”，不得全量注入整个知识库。

### AC-PJ-5: 项目实践沉淀

- 项目执行过程中，用户可把结论沉淀为项目知识条目；
- 条目至少包含：结论、适用范围、来源、时间；
- 结论类知识默认写入项目 notes 层，而非覆盖原始导入文件。

### AC-PJ-6: 项目隔离

- 项目 A 检索默认不得读取项目 B 私有知识；
- 所有索引与元数据必须按项目独立存储；
- 删除项目时，项目知识可随项目独立清理（用户确认后执行）。

### AC-PJ-7: 同类型项目复用（复制模式）

- 新项目可选择“导入历史项目知识快照”；
- 导入采用复制模式（新项目生成独立副本）；
- 导入后可单独编辑，不影响来源项目内容。

### AC-PJ-8: 孤儿项目检测与清理

- 当 `projects` 索引中存在项目，但 `projectRoot` 不存在时，系统必须将其识别为孤儿项目；
- 孤儿项需展示至少：项目名、原路径、聊天条数、Runtime 占用大小；
- 用户确认删除后，系统删除该项目在 Runtime 私有目录下的全部残留（含聊天记录与知识库）；
- 删除后项目需从 recent projects 索引移除，并记录操作日志（时间、项目、结果）。

### AC-PJ-9: 项目知识库迁移（导出/导入）

- 系统支持将项目知识库导出为迁移包（含 manifest 与完整校验信息）；
- 系统支持将迁移包导入目标项目，并重建目标项目索引；
- 导入后目标项目获得独立副本，不与源项目共享可变文件；
- 导入失败时必须回滚到导入前状态，并输出明确错误。

---

## 6. 技术设计 (Technical Design)

### 6.1 项目知识目录结构

建议目录（示意）：

```text
<runtime-userData>/runtime-store/projects/<projectId>/knowledge/
  source/                 # 用户导入原始文件
  extracted/              # 提取后的标准文本
  notes/                  # 项目实践沉淀（决策/术语/约束）
  snapshots/              # 从历史项目导入的快照（可选）
  backups/                # 孤儿清理前或迁移前备份（可选）
  index.json              # 本地检索索引元数据 / chunk catalog
  manifest.json           # 文件与索引元数据
  ops-log.ndjson          # 导入/孤儿清理/迁移操作日志
```

说明：该目录由 Runtime 管理，默认不暴露“直接编辑知识文件”的产品入口；用户通过“导入/沉淀/孤儿清理/迁移”流程操作知识。

`manifest.json` 建议最小字段：

```json
{
  "version": "1.0",
  "projectId": "xxx",
  "lastIndexedAt": "2026-03-09T00:00:00Z",
  "lastOrphanCleanupAt": null,
  "sources": [
    { "path": "source/policy.pdf", "mime": "application/pdf", "sha256": "...", "importedAt": "..." }
  ]
}
```

### 6.2 导入与索引流水线

1. 导入文件到 `source/`；
2. 按 MIME 分发提取器（PDF/图片、DOCX、文本）；
3. 写入 `extracted/` 标准文本；
4. 切片并写入 `index.json`；
5. 更新 `manifest.json` 与导入日志。

### 6.3 检索流水线

1. 基于用户 query 查询项目索引；
2. 按相关度返回 Top-K 片段；
3. 将片段与来源注入当前会话；
4. 在回答中保留引用来源，支持用户追溯。

### 6.4 同类项目复用（复制模式）

1. 用户选择历史项目与导入范围（全部或按标签）；
2. 系统复制 `notes/` 与可选 `extracted/` 到新项目；
3. 生成复用快照记录（来源项目、导入时间、导入项）；
4. 新项目重建索引并独立演化。

### 6.5 孤儿项目清理流程（复用现有聊天检测能力）

1. 复用 Runtime 现有 orphan 项目检测链路，判断 `projectRoot` 是否存在；
2. 当项目目录缺失时，聚合该项目 Runtime 残留统计（会话数、占用大小、知识库状态）；
3. 在 Orphan 数据面板中提供 `Rebind`、`Delete Data`、`Ignore` 操作；
4. 用户确认 `Delete Data` 后删除 `runtime-store/projects/<projectId>/` 整体目录（含聊天与知识库）；
5. 删除完成后从项目索引移除并写入操作日志。

### 6.6 迁移流程（导出/导入）

1. 导出：打包 `source/`、`extracted/`、`notes/`、`manifest.json` 为迁移包；
2. 导出包写入 `sha256` 校验与导出元数据（源项目、导出时间、版本）；
3. 导入：校验迁移包完整性后写入目标项目 `knowledge/`；
4. 导入完成后重建索引并写入迁移日志；
5. 任一环节失败时执行回滚，保证目标项目知识库一致性。

### 6.7 工具层建议（供实现）

- `kb.project.init`
- `kb.project.import`
- `kb.project.search`
- `kb.project.get`
- `kb.project.captureNote`
- `kb.project.importSnapshot`
- `projects.getOrphanProjects`（已有，复用）
- `projects.deleteOrphanProjectData`（已有，复用）
- `projects.rebindOrphanProject`（已有，复用）
- `projects.ignoreOrphanProject`（已有，复用）
- `kb.project.export`
- `kb.project.importArchive`
- `kb.project.rebuildIndex`

注：MVP 可先通过 Runtime 内部服务实现，工具暴露节奏可后置。

---

## 7. 非功能需求 (Non-Functional Requirements)

- **NFR-PJ-01 性能**：常见 query 检索响应目标 < 500ms（本地索引命中）；
- **NFR-PJ-02 稳定性**：单文件提取失败不得阻断整批导入；
- **NFR-PJ-03 可追溯性**：每个命中片段可追溯到原始文件；
- **NFR-PJ-04 安全性**：导入与读取遵循项目目录边界；
- **NFR-PJ-05 可维护性**：索引可随时重建，Markdown/文本仍可独立使用。
- **NFR-PJ-06 一致性**：孤儿清理与导入失败必须支持事务化回滚；
- **NFR-PJ-07 可迁移性**：迁移包在同版本 Runtime 间可稳定导入；
- **NFR-PJ-08 防篡改可见性**：导入文件与提取结果需支持完整性校验与异常提示。

---

## 8. 验证计划 (Validation Plan)

### 8.1 导入验证

- 导入混合文件集（PDF + DOCX + MD + 图片）可全部进入流水线；
- 每个文件在 `manifest.json` 中有记录；
- 提取失败文件返回可定位错误。

### 8.2 检索验证

- 使用项目关键术语检索，命中结果包含来源与片段；
- 回答中引用与实际来源一致；
- 大文件导入后检索性能满足目标。

### 8.3 复用验证

- 新项目导入历史快照后，检索可命中导入知识；
- 新项目修改导入内容不影响来源项目；
- 禁止跨项目直接读取未导入知识。

### 8.4 孤儿清理与迁移验证

- 删除项目工程目录后，系统可检测到孤儿项并展示聊天条数/占用大小；
- 执行删除后，Runtime 残留（聊天与知识库）被完整移除；
- 清理日志可追溯到操作者与时间；
- 导出后可在目标项目导入并重建索引；
- 导入损坏迁移包时系统拒绝并保持目标项目原状态。

---

## 9. 路线图 (Roadmap)

| 阶段 | 范围 | 目标 |
| :--- | :--- | :--- |
| Phase 1 | 空库初始化 + 文件导入 + 基础提取与检索 | 跑通项目知识闭环 |
| Phase 2 | 项目实践沉淀 + 快照导入复用 + 孤儿清理/迁移 | 提升项目连续性与治理能力 |
| Phase 3 | 质量评分 + 自动归档 + 与共享知识包对接 | 增强治理与复用效率 |
