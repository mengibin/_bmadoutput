# Runtime 模块功能清单与招标技术要求（对外版）

## 1. 文档说明

### 1.1 文档目的
本文件用于支持 Runtime 模块的采购、招标与供应商选型，形成统一的能力描述、技术要求、验收口径和评分标准，避免因口径不一致导致的比选偏差。

### 1.2 适用对象
- 采购部门
- 业务需求方
- 技术评审组
- 外部实施供应商

### 1.3 使用原则
- 以“可交付、可验收、可运维”为核心。
- 所有要求均应具备可测试性，不采用仅描述性条款。
- 供应商应对每一条要求给出“支持方式 + 交付证据 + 风险说明”。

---

## 2. Runtime 功能清单（细化版）

以下为当前目标 Runtime 模块应具备的业务能力项，按功能域进行拆分。

### 2.1 流程包管理与加载

#### F-01 导入流程包
- 支持通过图形界面导入标准流程包文件（ZIP 类格式）。
- 导入后自动解析包内元数据（名称、版本、流程列表、角色列表）。
- 导入失败时需返回明确原因（缺少关键文件、格式错误、版本不兼容等）。

#### F-02 流程结构解析
- 能读取流程图结构（节点、连线、入口节点）。
- 能读取流程步骤文件并建立“节点-步骤内容”的对应关系。
- 若步骤文件缺失或不可读，系统必须阻断运行并给出可定位错误信息。

#### F-03 流程合法性校验
- 校验流程入口节点是否存在。
- 校验连线引用的节点是否有效。
- 校验流程版本与系统支持版本是否匹配。
- 校验失败时禁止进入执行态。

---

### 2.2 运行与状态管理

#### F-04 运行实例创建
- 每次启动流程时创建独立运行实例。
- 实例需包含唯一标识、启动时间、流程标识、执行状态。
- 支持同一项目下存在多个历史运行实例。

#### F-05 运行状态读取与恢复
- 系统可读取当前步骤、已完成步骤、运行变量等状态信息。
- 应用重启后可恢复最近未完成运行的状态并继续展示进度。
- 当状态文件异常时，需给出“可恢复建议”而非静默失败。

#### F-06 进度可视化
- 基于流程定义与运行状态展示步骤进度：待执行/进行中/已完成。
- 流程推进后界面应自动刷新，避免用户手动反复刷新。

---

### 2.3 对话与执行可观测

#### F-07 对话记录持久化
- 会话记录、消息历史、会话类型需持久化保存。
- 重启后可按项目恢复历史会话。

#### F-08 执行日志面板
- 提供独立日志视图，不与主对话混杂。
- 至少支持：系统事件、思考过程、工具调用、错误信息。
- 支持自动滚动与日志数量上限控制，保证长期运行可用。

#### F-09 运行结果可追溯
- 支持按运行实例回看关键操作与输出结果。
- 运行日志应支持导出或归档（格式由供应商说明）。

---

### 2.4 项目文件管理与编辑

#### F-10 项目文件浏览
- 提供项目目录树浏览与快速定位能力。
- 目录变更应在低延迟内反映到界面。

#### F-11 文件预览与编辑
- 支持常见文本文件查看与编辑（如 Markdown、JSON、YAML、XML）。
- 提供保存能力和未保存状态提示。
- 支持在操作系统中打开目标文件。

#### F-12 表格文件编辑
- 支持 CSV、Excel 文件预览与编辑。
- Excel 场景需保留多工作表展示能力。
- 大表格需支持横向/纵向滚动并保持稳定交互。

#### F-13 文件操作
- 支持新建、重命名、删除、移动（含拖拽移动）。
- 删除与覆盖类操作必须二次确认。
- 同名冲突时必须提示并阻断误覆盖。

---

### 2.5 产物管理

#### F-14 产物落盘
- 流程执行产生的用户可见结果应写入项目目录中的产物区域。
- 自动创建不存在的父级目录。

#### F-15 产物登记
- 新产物生成后，系统应同步记录到该运行实例的产物列表。
- 产物列表应支持去重，避免重复记录。

---

### 2.6 脚本执行与扩展能力

#### F-16 Python 脚本执行
- 支持通过运行时内置 Python 执行代码片段或脚本文件。
- 返回标准输出、标准错误、退出码和耗时信息。
- 支持执行超时配置，超时后自动终止。

#### F-17 Python 依赖自动补齐
- 当脚本因缺少依赖失败时，系统可自动安装并重试。
- 支持开关控制自动安装行为。
- 支持配置镜像地址与最大重试次数。

---

### 2.7 LLM 配置与模型接入能力

#### F-18 LLM 服务商与模型配置
- 支持在设置界面配置主流大模型服务商（至少支持 OpenAI 兼容接口）。
- 支持配置服务地址、模型名称、API Key、请求超时等参数。
- 支持按环境（开发/测试/生产）或按项目进行独立配置。

#### F-19 连接测试与故障提示
- 提供“一键连接测试”能力，反馈连接是否可用、响应时延、错误原因。
- 当鉴权失败、地址错误、模型不可用时，需给出可操作的修复建议。

#### F-20 模型切换与降级策略
- 支持运行前或运行中切换模型配置（在不破坏会话数据的前提下）。
- 当主模型不可用时，支持预设备用模型或备用服务地址。
- 关键切换动作应写入审计日志，便于问题追溯。

---

### 2.8 MCP（外部工具协议）扩展能力

#### F-21 MCP 服务管理
- 支持新增、编辑、启停、删除 MCP 服务连接配置。
- 支持查看服务状态（已连接、连接中、失败）与最近错误信息。
- 支持按服务设置超时、重试和健康检查策略。

#### F-22 MCP 工具发现与调用
- 系统可自动发现已接入 MCP 服务提供的工具能力并展示给用户。
- 在执行过程中可调用外部工具并返回结构化结果。
- 工具调用失败时应具备重试、降级或跳过策略（按策略配置执行）。

#### F-23 MCP 权限与审计
- 可对 MCP 工具设置可见范围与调用权限（按用户、项目或会话维度）。
- 对关键工具调用记录审计日志（时间、调用对象、结果状态）。
- 支持对高风险外部工具执行“调用前确认”策略。

---

## 3. 招标技术要求（供应商必须响应）

### 3.1 功能性要求

#### R-01 功能覆盖要求
供应商须对第 2 章全部能力项给出“支持/部分支持/不支持”声明；“部分支持”必须提供替代方案与交付时限。

#### R-02 关键流程闭环
至少完成以下端到端闭环：
1. 导入流程包  
2. 创建运行实例  
3. 执行步骤并生成产物  
4. 查看进度与日志  
5. 重启后恢复运行状态  

#### R-03 失败场景可处理
对导入失败、流程文件损坏、脚本异常、文件冲突、模型连接异常、外部工具连接异常等场景，必须有明确提示与恢复路径。

---

### 3.2 安全性要求

#### R-04 目录边界控制
- 文件读写必须限制在授权目录范围内。
- 不得允许通过路径拼接、软链接或目录跳转访问未授权区域。

#### R-05 敏感信息保护
- 不得在界面、日志、错误文本中暴露用户敏感本地路径或密钥信息。
- 敏感数据须加密存储或受保护存储。

#### R-06 高风险操作防误用
- 删除、覆盖、批量改动等高风险操作必须具备确认机制。
- 关键操作应可追溯（至少记录操作者、时间、对象、结果）。

---

### 3.3 可靠性与可恢复要求

#### R-07 异常恢复
- 发生异常退出后，应能恢复最近运行状态与会话记录。
- 状态写入需具备一致性保障，避免出现“部分写入导致状态损坏”。

#### R-08 日志完整性
- 关键执行日志需持久化。
- 运行日志应支持按时间或运行实例进行查询。

#### R-09 兼容升级
- 版本升级后应保证历史项目可继续打开。
- 不兼容场景需提供迁移策略或自动修复方案。

---

### 3.4 性能要求

#### R-10 交互响应
- 常规页面操作响应时间建议不高于 1 秒。
- 项目文件目录变更在界面刷新上的延迟建议不高于 2 秒。

#### R-11 大项目稳定性
- 在包含大量文件的项目中仍能稳定浏览与操作。
- 必须提供目录忽略策略与资源占用控制策略。

#### R-12 日志与会话规模
- 长会话、长日志场景下界面不可明显卡顿。
- 应提供日志裁剪或归档机制。

---

### 3.5 可运维与可交付要求

#### R-13 交付内容
供应商至少交付：
- 可安装运行包（目标操作系统）
- 部署与运维文档
- 用户操作手册
- 管理员手册
- 接口/配置文档
- 测试报告（功能、性能、安全）

#### R-14 可观测与排障
- 提供故障定位所需日志与诊断信息采集方式。
- 提供常见故障处理手册（导入失败、执行失败、状态恢复失败等）。

#### R-15 服务保障
- 明确质保期、响应时效、故障分级与修复时限。
- 明确升级发布机制与回滚策略。

---

### 3.6 LLM 与 MCP 专项要求

#### R-16 LLM 配置完整性
- 必须提供图形化 LLM 配置界面，支持新增、修改、测试、启用/停用配置。
- 必须支持最少两套模型配置模板（主用 + 备用）。
- 配置变更需可审计、可回滚。

#### R-17 LLM 运行策略
- 必须支持温度、上下文长度、超时、重试次数等基础策略参数。
- 必须支持按任务类型选择模型策略（如对话类、执行类）。
- 必须提供模型不可用时的降级策略说明与演示。

#### R-18 MCP 接入能力
- 必须支持不少于 3 个 MCP 服务并行接入（验收环境下可模拟）。
- 必须支持服务级状态监控、断线重连与错误告警。
- 必须支持动态加载/卸载 MCP 工具，不要求重启客户端。

#### R-19 MCP 安全与治理
- 必须提供外部工具权限控制机制与默认最小权限策略。
- 必须支持高风险工具的调用审批或二次确认。
- 必须提供外部工具调用审计报表或可导出审计日志。

#### R-20 交付可验证性
- 供应商需提供 LLM 配置与 MCP 接入的操作手册、演示脚本与测试报告。
- 测试报告需覆盖成功路径、失败路径、降级路径三类场景。

---

## 4. 验收要求（建议作为合同附件）

### 4.1 验收方式
- 文档审查
- 演示验收
- 黑盒测试
- 压力/稳定性测试
- 安全测试

### 4.2 关键验收用例（最少）
1. **流程包导入验收**：导入成功、导入失败提示、元数据展示正确。  
2. **运行恢复验收**：执行中断后重启，进度与状态可恢复。  
3. **产物落盘验收**：执行结果落盘成功，产物列表可追溯。  
4. **文件操作验收**：新建/改名/删除/移动均生效，冲突处理正确。  
5. **脚本执行验收**：脚本执行、超时终止、缺依赖自动补齐。  
6. **LLM 配置验收**：模型配置可新增/测试/切换，主备切换策略有效。  
7. **MCP 扩展验收**：可接入外部工具服务，工具可发现、可调用、可审计。  

### 4.3 验收判定建议
- 关键条款（R-04、R-05、R-07、R-13、R-16、R-18）任一不达标：整体验收不通过。  
- 非关键条款允许限期整改，但需提交整改计划与复测安排。  

---

## 5. 供应商评分建议（100 分）

### 5.1 评分维度
- 功能符合度：35 分
- 安全与合规：25 分
- 稳定性与可恢复：15 分
- 性能与体验：10 分
- 交付与文档质量：10 分
- 服务与成本：5 分

### 5.2 否决项（建议）
- 不能完成目录边界控制
- 无法提供可验证的日志追溯能力
- 无正式交付文档或测试报告

---

## 6. 建议采购策略

### 6.1 分阶段招标（建议）
- 第一阶段：核心能力交付（第 2 章 F-01 至 F-15）  
- 第二阶段：增强能力与安全加固（第 2 章 F-16 至 F-23）  

### 6.2 合同里程碑（建议）
1. 需求冻结与方案评审通过  
2. 核心能力开发完成并通过联调  
3. 安全/稳定性测试通过  
4. 试运行与培训完成  
5. 终验与质保启动  

---

## 7. 附录：供应商应答模板（可复制）

### 7.1 功能应答
- 条款编号：  
- 是否满足（满足/部分满足/不满足）：  
- 实现说明：  
- 风险与限制：  
- 计划交付版本与日期：  

### 7.2 非功能应答
- 安全策略说明：  
- 稳定性设计说明：  
- 性能基线与测试结果：  
- 运维保障方案：  

### 7.3 验收证据清单
- 演示视频/截图  
- 测试报告  
- 关键日志样例  
- 用户手册与运维手册  
- 问题清单与整改闭环记录  
